# Using Boostrap Samples

-----------------------

Some aspects of these pages (especially links between pages) will
not work correctly unless you are running in a server. To start an
express based server, use:

    npm install
    node serve.js